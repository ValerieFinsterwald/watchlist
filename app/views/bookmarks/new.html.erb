<div class="container">
  <h1>Create Bookmark</h1>
  <%= simple_form_for [@list, @bookmark] do |f|%>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.hidden_field :list_id, value: params[:list_id] %>
  <div class="btn btn-rose"><%= f.button :submit %></div>
  <% end %>
</div>
